{ lib, config, pkgs, ... }:

{
    programs = {
        nixvim = {
            plugins = {
                
            };
        };
    };
}


{
    mode = "n";
    key = "<leader>";
    action = "<CMD><CR>";
    options.desc = "";
}